# CloudOps Change Guard — フロントエンド環境変数サンプル（Vite が読む）
# cp frontend/.env.example frontend/.env で実値を埋める。.env はコミット禁止（.gitignore 済）。
# VITE_ 接頭辞のみクライアントへ露出する。

# デモ用クイックログインボタンの表示。既定 on。
# 公開到達ビルドでは false を明示し、初期管理者パスワードを env で上書きする。
VITE_DEMO_LOGINS=true
